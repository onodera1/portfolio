<div class="container">
  <div class="row">
    <div class="col-lg-3"> 
    </div>
    <div class="col-lg-6"> 
		    <div class=head>投稿した共有一覧</div>
		    <% @sharings.each do |sharing| %>
		     <div class="panel panel-warning">
	         <div class="panel-body">
	  		     <%= link_to sharing.title,sharing_path(sharing.id) %>
				  </div> 
				 </div>
				<%end%>
				<%= link_to '戻る', :back %>
		</div>
		<div class="col-lg-3"> 
    </div>
  </div>
</div>

